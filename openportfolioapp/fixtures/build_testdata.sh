#!/bin/sh

echo  > initial_data.yaml
cat assetclass.yaml >> initial_data.yaml
cat gicssector.yaml >> initial_data.yaml
cat company.yaml >> initial_data.yaml
cat investment.yaml >> initial_data.yaml
cat savingsaccount.yaml >> initial_data.yaml
cat listedequity.yaml >> initial_data.yaml
cat portfolio.yaml >> initial_data.yaml
cat trade.yaml >> initial_data.yaml
cat currency.yaml >> initial_data.yaml


